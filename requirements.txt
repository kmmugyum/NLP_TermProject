# CNU Campus ChatBot — 의존성 (대상: Python 3.10.12 / torch 2.5.1, 그리고 Colab Free T4)
#
# torch 버전 전략 (두 타깃 공통 핀):
# - 베이스(Python 3.10.12): torch==2.5.1 (PDF p19 명세 충족, cu124 wheel) — 검증됨.
# - Colab(Python 3.12): 과거엔 사전설치 torch 유지 전략을 썼으나, Colab 이 torch 2.11.0+cu128 로
#   드리프트하면서 transformers 4.51.3 와 비호환이 됨(torch 2.7+ 에서 제거된
#   `flex_attention.TransformGetItemToIndex` 를 transformers 4.51.3 가 import → ImportError).
#   따라서 Colab 에서도 **검증된 torch==2.5.1 로 다운그레이드**하도록 환경마커를 제거해 무조건 핀.
#   (torch 2.5.1 은 cp312 cu124 wheel 존재. torchvision/torchaudio 는 미사용 → 진입점에서 제거해
#    2.11 잔재와의 버전 불일치를 방지. Qwen 4bit 안정성 위해 transformers 4.51.3 는 유지.)
# pytorch-lightning 2.4.0 은 코드 미사용(import 0건) → 명시 안 함(환경에 있으면 충돌 없이 무시).

# === Core (PDF 명세에 직접 영향) ===
torch==2.5.1   # 베이스(3.10)·Colab(3.12) 공통. Colab 사전설치 2.11 은 transformers 4.51.3 비호환 → 2.5.1 로 정합.
transformers==4.51.3
bitsandbytes>=0.46.1

# === LLM 보조 (사전설치 호환) ===
accelerate
peft
sentence-transformers
huggingface_hub
safetensors

# === Retrieval ===
faiss-cpu
rank_bm25
numpy
scikit-learn   # classifier.ipynb 의 F1 측정용

# === API / UI ===
fastapi
uvicorn[standard]
pydantic
httpx
beautifulsoup4
lxml            # BeautifulSoup(..., "lxml") 파서 — meal_crawler/notice/module4_api 13곳에서 사용. Colab은 사전설치되나 클린 py3.10 환경엔 없어 명시 필요.

# === 첨부파일 추출 (notice 본문 PDF/HWP) ===
PyMuPDF         # file_extractor.extract_pdf(fitz). 미설치 시 graceful 처리되나, 기능 동작엔 필요.
olefile         # file_extractor.extract_hwp. HWP(OLE) 파싱용. 미설치 시 graceful 처리.

# === 환경별 torch 처리 ===
# torch          — 위 Core 에서 모든 환경 2.5.1 핀. Colab 사전설치 2.11 은 진입점 pip 설치 시 다운그레이드됨.
# torchvision/torchaudio/torchao/torchcodec — 미사용. Colab 사전설치본은 torch 2.11 정합이라 2.5.1 과
#   불일치 → chatbot.sh/realtime_chatbot.sh/classifier.ipynb 가 선제 uninstall (import 충돌·dynamo mismatch 회피).
# classifier.ipynb — 노트북 커널은 torch 를 한 번 import 하면 메모리에 남아 다운그레이드가 반영 안 됨 →
#   Cell 0 이 deps 설치를 import 보다 먼저 수행하고, 그래도 비호환 torch 가 적재돼 있으면 런타임 1회 재시작.
# pytorch-lightning — 코드 미사용(import 0건). 환경에 있어도 무시.
